"""Setup-Konfiguration für das Projekt."""

from setuptools import find_packages, setup

setup(
    name="python_umgebung",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.13",
)
